<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
  </PropertyGroup>
  
  <!-- Define compile constant when PDFium is excluded -->
  <PropertyGroup Condition="'$(ExcludePdfium)' == 'true'">
    <DefineConstants>$(DefineConstants);EXCLUDE_PDFIUM</DefineConstants>
  </PropertyGroup>
  
  <ItemGroup>
    <AvaloniaResource Include="Assets\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" />
    <PackageReference Include="Avalonia.Themes.Fluent" />
    <PackageReference Include="Avalonia.Fonts.Inter" />
    <!--Condition below is needed to remove Avalonia.Diagnostics package from build output in Release configuration.-->
    <PackageReference Include="Avalonia.Diagnostics">
      <IncludeAssets Condition="'$(Configuration)' != 'Debug'">None</IncludeAssets>
      <PrivateAssets Condition="'$(Configuration)' != 'Debug'">All</PrivateAssets>
    </PackageReference>
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="SixLabors.ImageSharp" />
    <PackageReference Include="OpenCvSharp4" />
    <!-- For SQLite local storage -->
    <PackageReference Include="sqlite-net-pcl" />
    <PackageReference Include="SQLitePCLRaw.bundle_green" />
    <!-- For CBR (RAR) file support -->
    <PackageReference Include="SharpCompress" />
  </ItemGroup>

  <!-- PDFium is only needed on desktop platforms (Windows, macOS, Linux) -->
  <!-- It is not used on Android which has its own PDF renderer -->
  <ItemGroup Condition="'$(ExcludePdfium)' != 'true'">
    <PackageReference Include="PDFiumCore" />
  </ItemGroup>
  
  <!-- Exclude PdfiumPdfRenderer.cs when building for Android -->
  <ItemGroup Condition="'$(ExcludePdfium)' == 'true'">
    <Compile Remove="Services\PdfiumPdfRenderer.cs" />
  </ItemGroup>

  <!-- OpenCV runtime packages for desktop platforms -->
  <ItemGroup Condition="'$(ExcludePdfium)' != 'true'">
    <PackageReference Include="OpenCvSharp4.runtime.win" />
    <PackageReference Include="OpenCvSharp4.official.runtime.linux-x64" />
    <PackageReference Include="OpenCvSharp4.runtime.osx.10.15-universal" />
  </ItemGroup>

  <!-- OpenCV runtime package for Android -->
  <ItemGroup Condition="'$(ExcludePdfium)' == 'true'">
    <PackageReference Include="Sdcb.OpenCvSharp4.mini.runtime.android-arm64" />
  </ItemGroup>
</Project>
